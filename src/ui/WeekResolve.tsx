import { AnimatePresence, motion } from "framer-motion";
import type { GameState } from "../engine/types";
import { useGame } from "../store/useGame";
import { NumberValue } from "./Number";

export function WeekResolve({ game }: { game: GameState }) { const open=useGame((s)=>s.reportOpen), close=useGame((s)=>s.closeReport); const report=game.weeklyReports.at(-1); return <AnimatePresence>{open&&report&&<motion.div className="week-dimmer" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><motion.section className="week-report" initial={{opacity:0,y:50}} animate={{opacity:1,y:0}} transition={{duration:.35}}><span className="eyebrow">WEEK {report.week} RESOLVED</span><h2>{report.cashDelta>=0?"The company bought more time.":"Progress spent runway."}</h2><div className="report-numbers"><motion.article initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} transition={{delay:.25}}><span>OPERATING CHANGE</span><strong className={report.cashDelta>=0?"positive":"danger"}><NumberValue value={Math.abs(Math.round(report.cashDelta))} prefix={report.cashDelta>=0?"+$":"−$"}/></strong></motion.article><motion.article initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} transition={{delay:.45}}><span>NEW CUSTOMERS</span><strong><NumberValue value={report.newCustomers}/></strong></motion.article><motion.article initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} transition={{delay:.65}}><span>CHURNED</span><strong><NumberValue value={report.churned}/></strong></motion.article></div><div className="report-notes">{report.notes.length?report.notes.map((note,index)=><motion.p key={`${note}-${index}`} initial={{opacity:0,x:-12}} animate={{opacity:1,x:0}} transition={{delay:.75+index*.12}}><b>{String(index+1).padStart(2,"0")}</b>{note}</motion.p>):<p><b>01</b>The week was quiet. Quiet is still information.</p>}</div><button className="primary" onClick={close}>Continue to week {game.week} <span>→</span></button></motion.section></motion.div>}</AnimatePresence>; }

import { selectBurn, selectRunwayDisplay, selectWeeklyRevenue } from "../../engine/selectors";
import type { GameState } from "../../engine/types";
import { ActionList } from "../ActionList";
import { NumberValue } from "../Number";

export function CapitalPanel({ game }: { game: GameState }) { const burn=selectBurn(game), revenue=selectWeeklyRevenue(game); return <div className="panel-stack"><p className="panel-intro">Investors buy conviction first. The company still pays for the gap between conviction and evidence.</p><div className="metric-grid"><article><span>Runway</span><strong>{selectRunwayDisplay(game)}</strong><small>cash divided by net weekly burn</small></article><article><span>Valuation</span><strong><NumberValue value={Math.round(game.valuation)} prefix="$"/></strong><small>marked by MRR, reputation, conviction</small></article><article><span>Outside capital</span><strong><NumberValue value={Math.round(game.outsideCapital)} prefix="$"/></strong><small>{game.outsideCapital?"terms remain in the company":"fully founder-funded"}</small></article><article><span>Weekly truth</span><strong className={revenue-burn>=0?"positive":"danger"}><NumberValue value={Math.abs(Math.round(revenue-burn))} prefix={revenue-burn>=0?"+$":"−$"}/></strong><small>${Math.round(revenue).toLocaleString()} in · ${Math.round(burn).toLocaleString()} out</small></article></div><section className="conviction-block"><div><span>CONVICTION</span><strong>{Math.round(game.conviction)}</strong></div><div><span>EVIDENCE</span><strong>{Math.round(game.evidenceScore)}</strong></div><div><span>OVERCLAIM</span><strong className={game.overclaim>=10?"danger":""}>{Math.round(game.overclaim)}</strong></div></section><div className="section-title"><span className="eyebrow">CAPITAL</span><h3>Choose whose clock the company runs on</h3></div><ActionList game={game} group="capital"/></div>; }
